# Normalize line endings across platforms.
# Shell release checks must stay LF so they run on CI and POSIX shells.
scripts/*.sh text eol=lf
# Keep PDFs as binary to avoid CRLF renormalization corrupting them.
*.pdf binary
